# v1.1.0

Claude Token Management now ships as a polished public control plane for Claude Code token drain, not just a private runtime extraction.

## What This Release Locks In

- guard hooks, benchmarks, compatibility reporting, and certification aligned across live runtime and standalone repo
- fresh-runtime cert passing `10/10`
- live hook suite passing `481 passed, 37 skipped`
- live health-check passing `42 passed, 0 failed, 0 warnings`
- drain benchmark passing `9/9`
- schema validation passing `1,307` documents with `0` errors
- coordinator source-tree suite passing `316/316`

## Public Surface Improvements

- README rebuilt for cold GitHub traffic with pain, proof, quickstart, and demo assets
- launch pack added for GitHub and X
- OSS trust files added: license, contributing, security policy, and code of conduct
- issue templates and PR template added for reproducible intake

## Scope

This remains a local Claude Code control plane. It blocks avoidable waste, measures the rest, and tracks upstream regressions honestly instead of pretending upstream systems are solved locally.
