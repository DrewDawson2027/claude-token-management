# v1.1.1

This patch release makes the public launch surface match the repository's final launch-ready state.

## What Changed

- PNG exports added for launch card, X header, README hero, and runtime demo assets
- launch docs updated to prefer the PNG assets for platform upload flows
- package version files aligned with the current public release state

## Existing Proof Surface

- fresh-runtime cert: `10/10`
- live hooks: `481 passed, 37 skipped`
- live health-check: `42 passed, 0 failed, 0 warnings`
- drain benchmark: `9/9`
- schema validation: `1,307` documents, `0` errors
- coordinator suite: `316/316`

## Why This Exists

The project was already technically strong. This release closes the last packaging gap between a strong repository and a strong launch surface.
