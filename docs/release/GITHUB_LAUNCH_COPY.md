# GitHub Launch Copy

## Repo Description

Use this as the repository description:

```text
Local Claude Code token-drain control plane with guards, benchmarks, schemas, and live/runtime certification.
```

## Repo Intro Blurb

Use this anywhere a short project summary is needed:

```text
Claude Token Management is a public, self-contained control plane for Claude Code token usage. It blocks avoidable waste before spend lands, tracks known drain classes, and ships with fresh-runtime certs, live checks, schemas, and coordinator tests.
```

## Pinned Repo Blurb

Use this as the short line when pinning the repo on your profile:

```text
Measured guardrails for Claude Code token drain, with certs and live-runtime proof.
```

## GitHub Launch Order

1. Make sure the repo description matches the text above.
2. Pin the repository on the GitHub profile.
3. Keep the repository itself as the primary surface for launch messaging.
4. Keep `README.md` and `docs/analysis/regression-results.md` aligned with the current certification numbers before sending traffic.
